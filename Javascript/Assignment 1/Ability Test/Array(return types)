var emptyarray= [];
function arrayValuesTypes(arr){
  for( i= 0; i< arr.length; i++){
    var dataTypeofitem = typeof(arr[i]);
    emptyarray.push(dataTypeofitem);
    }
  
return emptyarray;
}
var finalarray= arrayValuesTypes([1,"sarika", "code rush", true, false])
console.log(finalarray);
